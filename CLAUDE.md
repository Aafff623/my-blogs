# CLAUDE.md

本文件包含 Claude Code 在本仓库中工作时的项目级指令。

## Agent skills

### Issue tracker

Issues 以 GitHub issue 的形式跟踪。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认的 canonical 标签词汇。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局：仓库根目录下有一个 `CONTEXT.md` 和 `docs/adr/`。详见 `docs/agents/domain.md`。
